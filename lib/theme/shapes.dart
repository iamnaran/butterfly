import 'package:flutter/material.dart';

class AppShapes {
  
  static const roundedRectangle = RoundedRectangleBorder(
    borderRadius: BorderRadius.all(Radius.circular(12)),
  );

  static const circle = CircleBorder();
}